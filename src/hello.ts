const a = 1;

function hello() {
  const name = 'Steve';
  // console.log(`Hello, ${name}`);
  Logger.log(`Hello, ${name}, ${a}, ${b}, ${c}`);
  test();
}

function newTest() {
  Logger.log(`newTest ${a}, ${b}, ${c}`);
}

const b = 2;
